#!/bin/bash
set -e

echo "Installing runtime {{.RuntimeSpec}} using direct script"

# Install dependencies first
{{if .Dependencies}}
echo "Installing dependencies: {{.Dependencies}}"
apt-get update && apt-get install -y {{.Dependencies}}
{{end}}

# Run setup script
echo "Executing setup script..."
{{.SetupScript}}

echo "Runtime {{.RuntimeSpec}} installation completed"