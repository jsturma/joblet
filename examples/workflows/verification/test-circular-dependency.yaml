jobs:
  job-a:
    command: "echo"
    args: [ "Job A" ]
    runtime: "python-3.11-ml"
    volumes: [ "data-pipeline" ]
    requires:
      - job-b: "COMPLETED"  # A depends on B
  
  job-b:
    command: "echo"
    args: [ "Job B" ]
    runtime: "python-3.11-ml"
    volumes: [ "data-pipeline" ]
    requires:
      - job-c: "COMPLETED"  # B depends on C

  job-c:
    command: "echo"
    args: [ "Job C" ]
    runtime: "python-3.11-ml"
    volumes: [ "data-pipeline" ]
    requires:
      - job-a: "COMPLETED"  # C depends on A -> creates cycle A->B->C->A