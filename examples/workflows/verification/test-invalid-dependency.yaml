jobs:
  job-a:
    command: "echo"
    args: [ "Job A" ]
    runtime: "python-3.11-ml"
    volumes: [ "data-pipeline" ]
    requires:
      - nonexistent-job: "COMPLETED"  # References job that doesn't exist
  
  job-b:
    command: "echo"
    args: [ "Job B" ]
    runtime: "python-3.11-ml"
    volumes: [ "data-pipeline" ]